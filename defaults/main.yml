---
bind_ldap_base: "cn=dns,dc=example,dc=org"
bind_ldap_url: "ldaps://127.0.0.1"
bind_ldap_krb_realm: "EXAMPLE.COM"
bind_ldap_ansible_url: "ldaps://127.0.0.1"

bind_dns_ldap_zoneid: "EXAMPLE"
bind_dns_zones: []
bind_dns_entries: []
bind_dnssec_keys: []
bind_rfc2136_keys: []
bind_notify_manual: False
bind_notify_manual_config: []

bind_group: "somegroup"
